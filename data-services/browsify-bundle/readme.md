This project will download NPM packages and then expose it in global vars so
you may import the generated `bundle.js` to use in browser.

    npm run build

Then copy `bundle.js` to your app.

See http://browserify.org/
