This mini web app is similar to "min-web-requirejs", except we use Grunt
to run r.js that minify main-optimized.js output file.

The output file will include all dependencies together!

NOTE: We still need to load requirejs before loading the optimize file!
