A simple HTML+JS web app that loads JS from local 'web' folder.
We will use RequireJS + Knockout + JQuery libraries to
see how it works in a browser env.

NOTE: The "web" folder is generated!

npm install
npm run build
